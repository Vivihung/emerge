[project]
name = "emerge-viz"
dynamic = ["version"]
description = "Visualize source code structure and dependencies in an interactive d3 application"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Grzegorz Lato", email = "grzegorz.lato@gmail.com" },
]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "autopep8",
    "coloredlogs",
    "interrogate",
    "networkx",
    "scikit-learn",
    "numpy",
    "prettytable",
    "py",
    "pycodestyle",
    "pylint",
    "pyparsing",
    "python-louvain",
    "PyYAML",
    "tabulate",
    "PyDriller",
    "pyperclip",
]

[project.urls]
Homepage = "https://github.com/glato/emerge"

[project.scripts]
emerge = "emerge.main:run"

[tool.hatch.version]
path = "emerge/appear.py"

[tool.hatch.build.targets.wheel]
packages = ["emerge"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
